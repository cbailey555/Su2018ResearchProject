use address::Address;
use errors::LibError;
use failure::Error;

/*
pub fn sender_eq_pubkey(_msg_pubkey: String, _check: String) -> Result<(), Error> {
    if msg_pubkey == _check {
        Ok(())
    } else {
        Err(Error::from(LibError::CustomErro { description: format!("Validation failed! this message has not been signed by someone with the proper credentials.")}))
    }
} 
*/
